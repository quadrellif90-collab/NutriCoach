# NutriCoach — requirements
# App gestionale nutrizione per nutrizionisti (localhost, SQLite)
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
PyMuPDF>=1.24.0
reportlab>=4.0.0
# OCR su PDF scansionati (opzionale: richiede anche il binario Tesseract installato nel sistema)
pytesseract>=0.3.10
Pillow>=10.0.0
# finestra nativa desktop (come PCC — apre l'app in una finestra, non nel browser)
pywebview>=6.0.0
# build EXE (sviluppo): pyinstaller NutriCoach.spec
pyinstaller>=6.0.0
