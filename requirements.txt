# NutriCoach — requirements
# App gestionale nutrizione per nutrizionisti (localhost, SQLite)
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
PyMuPDF>=1.24.0
reportlab>=4.0.0
# build EXE (sviluppo): pyinstaller NutriCoach.spec
pyinstaller>=6.0.0
